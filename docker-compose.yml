services:
  backend:
    build: ./senti2-backend
    env_file:
      - ./senti2-backend/.env
    restart: unless-stopped

  frontend:
    build: ./Senti2
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
